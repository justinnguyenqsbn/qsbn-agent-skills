---
description: Interview the user about a feature and write its PRD
argument-hint: <feature-description> [--deep]
---

Invoke the `qsbn-prd` skill now, passing `$ARGUMENTS` as the feature
description to interview about. If `--deep` is present, strip it from the
description and pass it through as the deeper problem-validation mode flag.
