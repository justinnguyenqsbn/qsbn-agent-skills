---
description: Implement a feature's spec test-first inside a specified code submodule
argument-hint: <ado-ticket-id | feature-id | feature-name> --repo <repo-name>
---

Invoke the `qsbn-tdd` skill now, passing `$ARGUMENTS` through as-is
(feature identifier plus which `src/<repo-name>/` submodule to work in).
