---
description: Match a feature to an existing epic or propose a new one, then move it there
argument-hint: <prd-description | feature-file | feature-slug>
---

Invoke the `qsbn-propose-epic` skill now, passing `$ARGUMENTS` as the
feature to assign (or reassign) to an epic.
