---
description: Create or update a feature's epic and stories in the configured tracker
argument-hint: <feature | feature-id> [--target <azure-devops|github|linear|local>]
---

Invoke the `qsbn-sync-to-tracker` skill now, passing `$ARGUMENTS` through
as-is (feature identifier plus any `--target` flag).
