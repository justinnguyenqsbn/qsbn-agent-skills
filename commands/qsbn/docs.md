---
description: Scout every code submodule and generate or refresh docs/architecture.md and docs/project-overview.md for the whole project
argument-hint: [scan|refresh|brief]
---

Invoke the `qsbn-docs` skill now, passing `$ARGUMENTS` as the mode to
resolve.
